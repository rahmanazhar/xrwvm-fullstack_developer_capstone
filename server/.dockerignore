# Keep the build context small and avoid copying host-specific artefacts into
# the image. frontend/build is deliberately excluded here because stage 1 of
# the Dockerfile rebuilds it - copying a host build would shadow it.
.venv/
**/__pycache__/
*.pyc
db.sqlite3
frontend/node_modules/
frontend/build/
static/
